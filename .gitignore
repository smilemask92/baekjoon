# 이렇게 #를 사용해서 주석


# 현재 폴더 이하 (하위폴더 내용 모두 포함) 아래 확장자를 가진 파일들 모두 (* = all)
*.c

# 현재 폴더 이하 (하위폴더 내용 모두 포함) 아래 <파일명.확장자> 모두
--matlabfile.m

# 현재 폴더에서만 (하위폴더 내용 미포함) 아래 <파일명.확장자> 모두
/matlabfile.m


# logs란 이름의 파일 또는 폴더와 그 내용들 (파일명 명확히 일치해야함)
passwordfolders--

# logs란 이름의 폴더와 그 내용들
logs/

# logs 폴더 바로 안의 debug.log와 .c 파일들
logs/debug.log
logs/*.c

# logs 폴더 바로 안, 또는 그 안의 다른 폴더(들) 안의 debug.log
logs/**/debug.log
passwordfolders/**/innerlog.log

# 다른 무시조건에 해당되더라도, 무시하지 않을 파일앞엔 !붙이기
!notignorethisfile.txt